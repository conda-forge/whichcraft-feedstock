{% set version = "0.5.2" %}
{% set gh_org = "pydanny" %}
{% set name = "whichcraft" %}
{% set sha256 = "fecddd531f237ffc5db8b215409afb18fa30300699064cca4817521b4fc81815" %}


package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - python

test:
  source_files:
    - test_whichcraft.py
  requires:
    - flake8
    - pytest
    - wheel
    - pytest-cov

about:
  home: https://github.com/{{ gh_org }}/{{ name }}
  license: BSD-3-Clause
  summary: >
            This package provides cross-platform cross-python shutil.which
            functionality.

extra:
  recipe-maintainers:
    - bollwyvl
    - pydanny
