{% set version = "0.5.0" %}
{% set gh_org = "pydanny" %}
{% set name = "whichcraft" %}
{% set sha256 = "cf8ed8efc1f275eefdd2dda317e85ceb45cd92aee40b94ed6ea4c69931992c50" %}


package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - python

test:
  source_files:
    - test_whichcraft.py
  requires:
    - flake8
    - pytest
    - wheel
    - pytest-cov

about:
  home: https://github.com/{{ gh_org }}/{{ name }}
  license: BSD-3-Clause
  summary: >
            This package provides cross-platform cross-python shutil.which
            functionality.

extra:
  recipe-maintainers:
    - bollwyvl
    - pydanny
